= Discussion

The benchmark omits noise, calibration drift, transport delay, thermal constraints, and hardware variation. It therefore cannot support a claim about real actuator accuracy or robustness. Its useful result is procedural: known inputs remain explicit, synthetic values are labeled, and missing physical evidence is not invented.

A future physical study would require calibrated sensors, repeated runs across ambient conditions, uncertainty intervals, a baseline controller, and a preregistered analysis procedure.
