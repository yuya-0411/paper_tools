= Introduction

Compact engineering papers should distinguish measured facts from generated examples. This manuscript therefore treats every benchmark value as synthetic demonstration data. Its objective is to provide a reproducible application fixture that includes a method, evaluation conditions, quantitative results, a figure, a table, and a real documentation reference.

The contribution is a deliberately small controller example whose assumptions are visible. It is not a claim of a new physical actuator or a comparison with published controllers.
