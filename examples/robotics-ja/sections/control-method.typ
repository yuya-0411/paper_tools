= 制御方法

左右sensorの平均反射強度を $r$ とする．低い閾値 $t_1$ と高い閾値 $t_2$ を用意し，$r < t_1$ では通常速度，$t_1 <= r < t_2$ では減速速度，$t_2 <= r$ では停止指令を与える．この切替則はsample説明用の単純化したmodelである．

\[VERIFY: 閾値の単位，sampling周期，filter処理を実機条件に合わせて確認する\]
